add_executable(SdCardFontTest
  SdCardFontTest.cpp
  ${PORT_ROOT}/test/host_shims/HalStorageHost.cpp
  ${PORT_ROOT}/vendor/lib/EpdFont/SdCardFont.cpp
  ${PORT_ROOT}/vendor/lib/EpdFont/EpdFont.cpp
  ${PORT_ROOT}/vendor/lib/EpdFont/EpdFontFamily.cpp
  ${PORT_ROOT}/vendor/lib/Logging/Logging.cpp
  ${PORT_ROOT}/vendor/lib/Utf8/Utf8.cpp
)

# host_shims FIRST so Arduino.h/HalStorage.h/HardwareSerial.h resolve to the
# RT-Thread-free host variants (same layout as the zipfile suite).
target_include_directories(SdCardFontTest PRIVATE
  ${PORT_ROOT}/test/host_shims
  ${PORT_ROOT}/port/arduino
  ${PORT_ROOT}/vendor/lib/EpdFont
  ${PORT_ROOT}/vendor/lib/Logging
  ${PORT_ROOT}/vendor/lib/Memory
  ${PORT_ROOT}/vendor/lib/Utf8
)

# Fixture generated by `uv run tools/build_cjk_font.py --fixture` (committed).
target_compile_definitions(SdCardFontTest PRIVATE
  FIXTURE_CPFONT="${PORT_ROOT}/test/fixtures/TestCJK_14.cpfont"
  ENABLE_SERIAL_LOG
  LOG_LEVEL=0
)

target_link_libraries(SdCardFontTest PRIVATE
  cp_test_common
  GTest::gtest_main
)

gtest_discover_tests(SdCardFontTest)
